// See https://aka.ms/new-console-template for more information
using Arrays.Aayushi;
using Arrays.GFG;
using Arrays.Strivers.Arrays;
using Arrays.Vivekayand;
using Programs.Strivers.Arrays;

internal class Program
{
    public static void Main(string[] args)
    {
        #region strivers 


        //Largestemelemt.FindLargestElement();
        //SecondLargest.SecondLargestValue();
        //SecondLargest.SecondSmallest();
        //Triplet.find(); 
        //Rotatetoleft.LeftRotateByOne();
        //Rotatetoleft.LeftRotateByK();
        //MoveZerostoEnd.MoveBrute();
        //MoveZerostoEnd.MoveOptimal();
        //FindUnion.UnionBrute();
        //FindUnion.UnionOptimal();
        //findIntersection.intersetionBrute();
        //findIntersection.intersetionOptimal();

        #endregion


        #region GFG 
        //FirstRepeatingElement.firstreepeting();


        //#region SearchDelete_Insert
        //int[] arr = { 10, 20, 30, 40, 50 };


        ////Searching an element using binary search
        //int n = arr.Length;
        //int key = 30;
        //Console.WriteLine("Index: " + SearchDelete_Insert.Search(arr, 0, n - 1, key));

        ////inserting an element
        //int[] arr1 = { 10, 20, 30, 40, 50, 0 };
        //int n1 = arr1.Length;
        //key = 26;

        //Console.WriteLine("nBefore Insertion: ");
        //for (int i = 0; i < n; i++)
        //    Console.WriteLine(arr1[i]);

        //n1 = SearchDelete_Insert.Insert(arr1, n1, key);

        //Console.WriteLine("nAfter Insertion: ");

        //for (int i = 0; i < n1; i++)
        //    Console.WriteLine(arr1[i]);


        ////using linq
        ////int searchIndex = Search(arr, key);
        ////Console.WriteLine(searchIndex != -1 ? $"Element {key} found at index {searchIndex}" : "Element not found");


        ////arr = SearchDelete_Insert.Insert(arr, key);
        ////Console.WriteLine("After Insertion: " + string.Join(", ", arr));


        ////arr = Delete(arr, key);
        ////Console.WriteLine("After Deletion: " + string.Join(", ", arr));

        ////deleting an elment
        //key = 30;
        //n = SearchDelete_Insert.Delete(arr, n, key);
        //Console.WriteLine("delete Insertion: ");
        //for (int i = 0; i < n; i++)
        //    Console.WriteLine(arr[i]);
        //#endregion SearchDelete_Insert



        #endregion GFG



        #region vivekayanad
        //Equilibrium.EquiIndex();
        #endregion

        #region Aayushi
        //_4Sum.SumOptimal();
        //NextPermutation.findNext();
        LongestConsecutiveSequence.FindLongest();
        #endregion
    }
}
